services:
  nginx:
    image: nginx:stable
    container_name: automa-nginx
    environment:
      VIRTUAL_HOST: automa.lcl
      VIRTUAL_PORT: 80
      VIRTUAL_PROTO: http
      # vhost-proxy picks the key pair up by this name; ./docker-install-certs.sh
      # copies docker-certs/automa.lcl.{crt,key} into it.
      CERT_NAME: automa.lcl
    expose:
      - "80"
      - "443"
    volumes:
      - ./:/usr/share/nginx/html:cached
    networks:
      - automa-net
      - proxy-net  # attach to vhost-proxy network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  automa-net:
    driver: bridge
    name: automa-net
  proxy-net:
    external: true
    name: proxy-net
