# Normalize all text to LF in the repo; editors on Windows/WSL keep reintroducing CRLF
# into cards/*.json, which produces 850-line phantom diffs. See AGENTS.md C2.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
